# Wall-X 视觉 Token 筛选成果定位

项目目录：`/root/autodl-tmp/wall_x`  
报告主题：大创项目成果展示口径与指标选择

## 1. 成果定位

本项目的核心成果可以定位为：在 Wall-X 机器人视觉语言动作模型中，完成视觉 token 筛选机制的移植、接入、开关化配置与性能诊断。当前工作重点不是宣称端到端推理获得大幅加速，而是证明视觉 token 筛选链路已经真实生效，并分析其在 Wall-X 动作推理流程中的收益边界与瓶颈来源。

建议表述：

> 本项目实现了 Wall-X 视觉 token 筛选功能，使主模型实际处理的视觉 token 数和序列长度明显下降；同时通过分段计时定位到端到端加速受限的主要原因是 ODE/postfix Transformer 动作生成阶段占主导。

## 2. 推荐展示指标

### 2.1 视觉 Token 削减比例

该指标最直接证明视觉 token 筛选功能有效，适合作为成果展示的主结果。

| 指标 | 原始版本 | 筛选版本 | 变化 | 展示价值 |
|---|---:|---:|---:|---|
| 平均视觉 token 数 | 63.00 | 32.00 | 减少 49.21% | 证明筛选逻辑真实生效，视觉 token 数接近减半。 |

### 2.2 主模型序列长度下降

如果采用迁移报告中的单图 shape check 数据，可以展示主 Transformer 实际处理序列长度明显下降，说明剪枝不是只发生在统计层面，而是已经影响到模型输入序列。

| 指标 | 原始版本 | 筛选版本 | 变化 | 展示价值 |
|---|---:|---:|---:|---|
| 输出 logits 序列长度 | 476 | 281 | 下降约 40.97% | 证明主模型处理序列被实际缩短。 |

### 2.3 视觉相关局部阶段加速

这些局部指标比端到端总时长更能体现视觉 token 筛选对相关模块的直接影响。展示时应说明这些是局部模块收益，而不是完整推理收益。

| 阶段 | 原始版本 | 筛选版本 | 变化 | 说明 |
|---|---:|---:|---:|---|
| `scatter_image_embeds` | 0.139 ms | 0.111 ms | 下降 20.14% | 视觉 token 减少后，图像特征写入输入 embedding 的工作量降低。 |
| `position_encoding` | 0.698 ms | 0.120 ms | 下降 82.82% | 序列变短后，位置编码与相关索引准备阶段耗时下降明显。 |
| `cache_preprocessing` | 1.387 ms | 1.348 ms | 下降 2.86% | KV-cache 和 postfix 输入准备阶段有小幅收益。 |

### 2.4 Pre-ODE 视觉相关链路收益

可以将视觉 token 直接影响的 pre-ODE 链路合并展示，口径如下：

```text
scatter_image_embeds
+ position_encoding
+ prefetch_forward
+ cache_preprocessing
```

| 统计口径 | 原始版本 | 筛选版本 | 变化 |
|---|---:|---:|---:|
| Pre-ODE 视觉相关链路 | 33.129 ms | 32.721 ms | 减少 0.408 ms，约 1.23% |

该指标比完整端到端总时间更贴近视觉 token 筛选的作用范围，适合用于解释“局部链路已经产生收益”。

### 2.5 筛选模块自身开销

当前筛选模块自身开销较低，可以作为工程实现有效性的一个补充指标。

| 指标 | 数值 | 占筛选版本总时间比例 | 说明 |
|---|---:|---:|---|
| `vispruner_total` | 0.925 ms | 约 0.27% | 筛选模块本身耗时低于 1 ms，占完整模型内部推理时间比例较小。 |

## 3. 端到端结果与合理解释

端到端模型内部总时间应作为局限性和瓶颈分析展示，避免只挑选局部指标。

| 指标 | 原始版本 | 筛选版本 | 变化 | 结论 |
|---|---:|---:|---:|---|
| `total_time` | 346.291 ms | 345.729 ms | 下降 0.562 ms，约 0.16% | 端到端加速不明显。 |

端到端加速有限的主要原因如下：

- 当前视觉 token 筛选发生在 vision tower 完整编码之后，不能节省视觉编码本身。
- 本次测试中视觉 token 数从 63 降到 32，仅减少 31 个 token，规模较小。
- Wall-X 动作推理主要耗时集中在 ODE/postfix Transformer 阶段，该阶段约占总时间 83%。
- ODE 阶段主要围绕 action token 反复运行 Transformer，视觉 token 只作为 prefix KV-cache 条件上下文，因此对视觉 token 数不够敏感。

## 4. 推荐汇报结构

1. **功能有效性：**视觉 token 数减少 49.21%，主模型输入序列长度明显下降。
2. **局部性能收益：**`scatter_image_embeds` 下降 20.14%，`position_encoding` 下降 82.82%。
3. **工程开销可控：**`vispruner_total` 约 0.925 ms，占总推理时间约 0.27%。
4. **端到端瓶颈分析：**总时长仅下降 0.16%，主要因为 ODE/postfix Transformer 动作生成阶段占主导。
5. **后续优化方向：**探索更早阶段的视觉剪枝、更高 token 数场景、多视角真实机器人数据，以及减少 ODE step 的联合优化。

## 5. 建议使用的汇报表述

```text
本项目完成了 Wall-X 视觉 token 筛选机制的移植与验证。
实验结果显示，在 30 张测试图片上，视觉 token 数平均从 63 降至 32，减少 49.21%；
在单图 shape check 中，主模型处理序列长度从 476 降至 281，下降约 40.97%。

在局部性能上，图像 embedding 写入阶段耗时下降 20.14%，位置编码阶段耗时下降 82.82%，
筛选模块自身耗时约 0.925 ms，占完整模型内部推理时间不到 0.3%。

同时，实验也发现 Wall-X 端到端动作推理的主要瓶颈并不在视觉 token 处理阶段，
而在 ODE/postfix Transformer 动作生成阶段。因此当前视觉 token 筛选对完整推理时间的提升有限，
但为后续结合高分辨率多视角输入、早期视觉剪枝和 ODE step 优化提供了清晰的工程基础与性能诊断依据。
```

## 6. 后续补充实验建议

- 使用真实机器人多视角图像替代随机自然图片，以验证任务场景下的剪枝稳定性。
- 提高输入分辨率或增加多视角图片数量，使原始视觉 token 数达到几百级别，再观察 prefill 阶段收益。
- 对比 `keep_ratio=0.75`、`0.5`、`0.25` 下的 token 数、局部耗时和动作输出漂移。
- 联合测试减少 `num_inference_timesteps` 后的端到端加速效果，区分视觉筛选收益和 ODE step 减少收益。
